/**
 * Svelte actions for Cloumask.
 *
 * Actions are reusable behaviors that can be attached to DOM elements
 * using the `use:` directive in Svelte components.
 */

export { shortcut } from "./shortcut";
