# Cloumask Backend - Computer Vision Dependencies
# Install with: pip install -r requirements-cv.txt
# Used in 03-cv-models module

-r requirements.txt

# PyTorch (CUDA 12.x)
torch==2.5.1
torchvision==0.20.1

# OpenCV
opencv-python==4.10.0.84

# Image processing
numpy==2.1.3
pillow==11.0.0
albumentations>=1.4.0

# Model loading
huggingface-hub==0.26.5
safetensors==0.4.5

# Ultralytics (YOLO11, RT-DETR)
ultralytics>=8.3.237

# InsightFace (SCRFD face detection)
insightface>=0.7.3
onnxruntime>=1.16.0

# ONNX Runtime GPU (optional, for optimized inference on GPU)
# onnxruntime-gpu==1.20.0

# Point Cloud Processing (3D detection)
open3d>=0.18.0
laspy>=2.5.0

# OpenPCDet (3D object detection with PV-RCNN++, CenterPoint)
# Note: OpenPCDet requires source installation with CUDA compilation
# See models/README.md for installation instructions
